server {
    listen 80;
    server_name localhost;

    location / {
        include uwsgi_params;
        uwsgi_pass unix:/tmp/metasploitmcp.sock;
    }

    location /static {
        alias /home/devpen11/Desktop/MetasploitMCP/static;
    }

    error_log /var/log/nginx/metasploitmcp_error.log;
    access_log /var/log/nginx/metasploitmcp_access.log;
}
